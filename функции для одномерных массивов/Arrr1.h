#pragma once
void printArr(int* A, const size_t);
void initArray(int* A, const size_t);
void initArrayRand(int* A, const size_t);
int findMax(int* A, const size_t);
int findMin(int* A, const size_t);
void findMaxMin(int* A, const size_t);
int sumArray(int* A, const size_t);
void positiveNegativeArray(int* A, const size_t);
void stupidSort(int* A, const size_t);
void giveMemory(int* A, const size_t);
void freeMemory(int* A, const size_t);